name: Variable Management Using GH CLI

on: workflow_dispatch

jobs:
  read_variable_list:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Reading variables..."
          echo "${MULTILINE_VAR}"
        env:
          MULTILINE_VAR: ${{ vars.MULTILINE_VAR }}

